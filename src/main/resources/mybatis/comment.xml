<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.model.mapper.CommentMapper">

	<select id="commentlist" parameterType="int" resultType="CommentDTO">
		SELECT c.title, c.content, c.score, p.thumbnail, p.title, m.nickname
		FROM comment AS c, post AS p, member AS m
		WHERE c.member_num = #{num} AND c.post_num = p.post_num AND m.num = p.post_num
	</select>
</mapper>